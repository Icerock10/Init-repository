{
    "extends": "../tsconfig.json",
    "exclude": ["node_modules", "build"],
    "compilerOptions": {
        "lib": ["DOM", "DOM.Iterable", "ESNext"],
        "jsx": "react-jsx",
        "baseUrl": ".",
        "paths": {
            "~/*": ["./src/*"]
        }
    }
}
